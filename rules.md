# قوانین و استانداردهای توسعه اپلیکیشن موبایل ترازینو (Tarazino)
# Mobile Ergonomics, RTL Standards & Operational Guidelines (`rules.md`)

این مستند استانداردهای اجباری پیاده‌سازی رابط کاربری موبایل، رفتار تعاملی، تایپوگرافی فارسی، دقت محاسبات و قوانین گردش کار گیت را مشخص می‌کند.

---

## ۱. استانداردهای ارگونومی لمسی و موبایل (Mobile Ergonomics & Touch Guidelines)

### ۱.۱. ابعاد هدف‌های لمسی (Minimum 48×48px Touch Targets)
- تمامی دکمه‌ها، آیکون‌های تعاملی، چک‌باکس‌ها، چیپ‌ها و دکمه‌های بستن مدال‌ها باید حداقل دارای محدوده لمسی **۴۸×۴۸ پیکسل** (`min-h-[48px] min-w-[48px]` یا پدینگ معادل با کلاس `touch-target`) باشند.
- فاصله بین عناصر کلیک‌پذیر مجاور حداقل ۸ پیکسل باشد تا از خطای لمسی کاربر با انگشت شست (Fat-finger syndrome) جلوگیری شود.

### ۱.۲. مدیریت حاشیه‌های امن ناوبری (Safe Area Insets Mastery)
- رعایت حاشیه‌های امن اندروید و iOS:
  - بالای صفحه: `pt-[max(1rem,env(safe-area-inset-top))]`
  - پایین صفحه: `pb-[max(1rem,env(safe-area-inset-bottom))]`
- نوار ناوبری پایینی (`BottomNav`) و دکمه‌های چسبنده (Sticky Action Buttons) هرگز نباید پشت نوار ژست‌های حرکتی سیستم‌عامل (Gesture Pill) پنهان شوند یا روی محتوای قابل اسکرول بیفتند (همیشه `pb-24` به محتوای اصلی اضافه شود).

### ۱.۳. منطقه دسترسی شست دست (Thumb Zone Ergonomics)
- مهم‌ترین دکمه‌های اقدام اصلی (Primary Actions / CTAs) باید در نیمه پایینی صفحه قرار گیرند.
- فیلدهای ورودی هنگام فوکوس روی موبایل به صورت خودکار به محدوده دید کاربر منتقل شوند تا کیبورد مجازی اندروید آن‌ها را نپوشاند.

### ۱.۴. بازخورد لرزشی و تعاملی (Tactile Haptic Feedback)
- در اقدامات کلیدی (کپی کردن مقادیر، ذخیره تغییرات، حذف آیتم‌ها، جابجایی بین تب‌ها) متد `triggerHaptic` فراخوانی شود (`light` برای تب‌ها، `medium` برای ذخیره و کپی، `heavy` یا `success` برای تایید نهایی).
- دکمه‌ها دارای افکت فشرده‌شدن ملایم باشند (`active:scale-[0.97]` یا `interactive-tap`).

---

## ۲. استانداردهای زبان فارسی، راست‌چین و قالب‌بندی اعداد (Persian RTL & Number Formatting)

### ۲.۱. ساختار راست‌چین (RTL Layout)
- جهت کلی برنامه `dir="rtl"` است. آیکون‌های جهت‌دار (مانند فلش‌های بازگشت و رفتن) باید متناسب با جهت راست‌چین تنظیم شوند.
- متون توضیحی و نام دارایی‌ها کاملاً راست‌چین و خوانا با فونت `Vazirmatn` نمایش داده شوند.

### ۲.۲. قالب‌بندی اعداد و واحدها
- مقادیر عددی، قیمت‌ها، مبالغ و درصدها در داخل محفظه‌های با کلاس `dir-ltr text-right` یا `text-left` قرار گیرند تا ترتیب کاراکترها و نمادها (`%` و `$`) به هم نریزد.
- مبالغ ریالی و تومانی با جداکننده سه‌رقمی استاندارد (`Intl.NumberFormat('en-US')`) قالب‌بندی شوند.
- واحدهای پولی به وضوح نمایش داده شوند: `تومان` یا مخفف `ت`، `ریال`، و دلار `$`.

---

## ۳. قابلیت اطمینان و ذخیره‌سازی آفلاین (Offline-First Architecture)
- تمامی داده‌ها (دارایی‌ها، املاک، تراکنش‌ها، تنظیمات و کلیدهای پیکربندی) به صورت ۱۰۰٪ محلی در `localStorage` پایدار می‌شوند.
- برنامه در صورت قطع بودن اینترنت، فیلترینگ یا انسداد وب‌سرویس‌های خارجی، بدون کرش کردن اجرا شده و آخرین نرخ‌های ذخیره‌شده را به همراه پیام هشدار متنی ملایم و امکان ثبت دستی نرخ‌ها نمایش می‌دهد.
- خروجی و ورودی فایل پشتیبان (`AppBackupData`) باید بدون از دست رفتن هیچ فیلدی به صورت بازگشتی کار کند.

---

## ۴. کنترل کیفی و توالی کامیت‌ها (Git Workflow & Quality Gate)
- **قانون عدم دسترسی به Main**: هرگونه تغییر فقط روی شاخه‌های فیچر (مانند `feature/property-gold-pnl-mobile-v2`) مجاز است.
- پس از پایان هر لوپ کاری، ابتدا `npm run build` با خروجی بدون خطا اجرا شده و سپس کامیت مرتبط ثبت گردد.
- پس از هر کامیت، دستور یادگیری سیستم فراخوانی شود.
